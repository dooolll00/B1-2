# B1-2 작업 기록

> 이 파일은 Codex 채팅이 사라져도 프로젝트 작업 흐름을 이어가기 위한 기록입니다.
> Codex는 작업 시작 시 이 파일을 읽고, 작업 종료 시 새 기록을 맨 아래에 추가합니다.

## 프로젝트 상태

- 프로젝트: B1-2
- 상태: 기존 React SPA 수정/개선 예정
- 기본 브랜치: `main`
- 주요 환경: React/Vite, Supabase, Vercel
- 작업 원칙: 기존 기능과 데이터를 보존하면서 필요한 부분만 수정
- 주의: `.env`의 실제 키나 비밀값은 작업 기록에 남기지 않는다.

---

## 2026-09-07 — 작업 기록 시스템 시작

### 요청

- 앞으로 Codex 작업 기록이 사라져도 이전 작업 내용을 이어갈 수 있도록 기록 체계를 만든다.

### 변경 파일

- `AGENTS.md` 추가
- `WORK_LOG.md` 추가

### 현재 알고 있는 프로젝트 맥락

- React SPA 프로젝트이다.
- Supabase 연동을 사용한다.
- Vercel 배포 흐름을 사용한다.
- CRUD, 상세 페이지 새로고침, 오류 처리 등을 점검하는 작업 흐름이 있다.

### 다음 작업

- B1-2 수정 작업을 할 때 이 기록을 먼저 읽고 기존 기능을 보존한 채 이어서 작업한다.

## 2026-09-07 18:56 KST — B1-2 사전 검토와 발표 문서 보완

### 요청

- 미션 요구사항 충족 여부 검토, README 체크리스트 추가, 설명·발표 준비 보완.

### 변경 파일

- `README.md`: 코드 근거가 있는 체크리스트, 검토 결과와 검증 한계 추가. 기존 배포 성공 주장을 과거 기록으로 명시.
- `PRESENTATION_GUIDE.md`: 3~5분 발표 대본, 데이터 흐름, 시연 순서, 예상 질문, 발표 준비 항목 추가.
- `WORK_LOG.md`: 이번 작업 기록 추가.

### 핵심 작업과 결정

- React 라우트, 공통 폼, 원격 CRUD, 비동기 훅, 인증, RLS SQL, 기존 테스트와 CI를 검토했다.
- 미션 원문은 저장소에 없고 사용자에게 요청한 상태이므로 완전 충족 판정을 보류했다.
- 기존 앱 코드를 변경하지 않았다. 과거 배포 검증과 이번 코드·로컬 검증을 구분했다.
- 목록 기반 검색·통계의 조회 한도, 익명 계정 복구 불가, mock 테스트의 범위를 발표 자료에 설명했다.
- 파일 검색에서 lockfile이 누락된 것으로 오인했으나 실제 파일과 Git 추적을 확인하고 사용자에게 정정했다.

### 테스트와 명령

- 최초 npm 명령은 시스템 Node.js/npm 부재로 실패했다.
- 공식 Node.js 22.14.0 darwin-x64 배포본을 `/tmp`에 받아 임시 PATH로 실행했다. 시스템 설치나 프로젝트 의존성 버전 변경은 하지 않았다.
- `npm ci`: 성공, 설치 시 감사 결과 취약점 0개.
- `npm test`: 기존 테스트 10개 모두 통과.
- `npm run build`: 성공.
- 실제 Supabase와 배포 사이트의 CRUD·RLS·모바일 재검증은 수행하지 않았다.

### 해결되지 않은 문제와 다음 작업

- 미션 원문을 받으면 필수/선택 항목을 정확히 대조하고 필요한 구현·테스트를 보완한다.
- 수정 성공 후 재조회, 삭제 취소, 상태 필터 조합, 앱 404는 현재 자동 테스트의 직접 검증 범위 밖이다.
- 실제 배포에서 CRUD, 상세 새로고침, 오프라인 재시도, 사용자 격리, 모바일을 확인한다.
- Git commit/push는 수행하지 않았다.

### 최종 형식 검증

- 변경 문서에 Prettier를 적용했다. `npm run format:check`, `git diff --check` 모두 통과했다.

## 2026-09-07 19:02 KST — 미션 원문 대조 및 실제 배포 검증

### 요청

- 제공된 React SPA 미션 전체 필수 요구사항과 보너스를 대조하고, README 체크리스트와 발표 준비를 마무리한다.

### 변경 파일

- `src/App.jsx` 추가: 실제 앱 라우트와 ToastProvider 조합을 진입점에서 분리하여 테스트에서 같은 라우트를 사용.
- `src/main.jsx`: StrictMode/BrowserRouter 안에서 App 렌더링. 기존 경로와 화면 유지.
- `src/test/flows.test.jsx`: 실제 라우트 7개(6페이지+404), 공통 메뉴, 수정 성공 후 상세 재조회, 삭제 취소, 필터 조합, 상세·수정 오류 재시도, 없는 수정 데이터 테스트 추가.
- `README.md`: 미션 원문 기준 필수 체크리스트, 재사용 컴포넌트 12개와 prop 근거 표, 보너스 판정, 실제 검증 결과 추가.
- `PRESENTATION_GUIDE.md`: 상태 소유 위치, props 콜백 흐름, effect 의존성, 렌더링 설명, 평가 기준에 맞춘 발표 마무리 추가.
- `WORK_LOG.md`: 본 기록 추가.

### 핵심 결과와 결정

- 필수 요구사항에서 누락을 발견하지 못했다. 전역 알림·메모이제이션 보너스 구현. 익명 Auth는 있으나 보호 라우트가 없어 인증 보너스 전체 충족으로 표시하지 않았다.
- 페이지네이션, 서버 검색, 영구 계정은 미션 필수가 아니므로 범위를 늘리지 않았다.
- 기존 공개 배포와 이번 로컬 작업 트리의 검증 범위를 문서에 구분했다.

### 실행 명령과 결과

- `/tmp/node-v22.14.0-darwin-x64/bin`을 임시 PATH로 사용.
- `npm test`: 처음 추가 테스트 중 Guide 제목 기대값 오류로 1개 실패. 실제 제목으로 수정 후 총 23개 통과.
- `npm run build`: 성공.
- Playwright를 `/tmp/b1-2-browser`에 설치하고 Chromium을 사용자 캐시에 설치. 프로젝트 의존성 변경 없음.
- `node /tmp/b1-2-browser/probe.cjs`, `node /tmp/b1-2-browser/verify.cjs`: 기존 공개 배포에서 목록, 빈 상태, 입력 검증, 미리보기, 등록, 상세 새로고침, 수정 영속성, 검색/초기화, 오프라인 조회 오류/재시도, 다른 세션 상세 미노출, 삭제 취소, 삭제 후 부재, 404, 390px 등록 폼 가로 넘침 없음, 미처리 브라우저 오류 없음 확인.
- 실제 테스트 기록 삭제 완료. 익명 Auth 테스트 계정 3개는 서버에 남음. 기존 사용자 기록을 수정·삭제하지 않음.
- GitHub 소스 URL HTTP 200 확인.
- 현재 Git 추적 파일에서 키/JWT/개인키 헤더 패턴 일치 0개, 환경 파일은 `.env.example`만 추적 확인. 비밀값 출력·기록하지 않음.

### 남은 사항과 다음 작업

- 이번 변경은 commit/push/재배포하지 않았다. 제출에 반영하려면 변경 파일을 공유 저장소와 배포에 반영한다.
- 인증 보너스를 추가로 수행하려면 로그인 UX와 보호 라우트를 별도 구현한다(선택).
- 이번 원격 사용자 격리 검증은 읽기만 수행. 실제 권한 위반 수정/삭제와 저장 실패 UI는 원격에서 재검증하지 않음(저장 실패는 mock 테스트 검증).
- 발표는 PRESENTATION_GUIDE 순서로 연습한다.

### 종료 전 확인

- 변경 파일 Prettier 정렬 후 `npm run format:check`, `git diff --check` 모두 통과했다.

## 2026-09-07 19:12 KST — 검토·발표 준비 변경 GitHub 반영

- 요청: 앞서 완료한 B1-2 변경 사항을 GitHub에 push한다.
- 변경 파일: `README.md`, `PRESENTATION_GUIDE.md`, `WORK_LOG.md`, `src/App.jsx`, `src/main.jsx`, `src/test/flows.test.jsx`.
- 핵심 작업: 기존 변경 6개 파일을 main 브랜치에 커밋해 origin으로 반영한다. README의 미푸시 안내를 검증 당시의 상태로 명확히 했다.
- 확인: `git fetch origin` 성공, HEAD와 origin/main 차이 0/0, `git diff --check` 통과. 앞선 코드 검증에서 테스트 23개·빌드·형식 검사 통과.
- 커밋 메시지: `feat: complete mission review docs and route flow coverage`.
- push 상태: 아래 후속 기록에 실제 실행 결과를 남긴다.
- 남은 사항/다음 작업: push 결과 확인. 자동 배포 성공 여부는 별도이며 이번 요청에서 배포 완료로 간주하지 않는다.

### GitHub 반영 결과 — 2026-09-07 19:14 KST

- 일반 `git push origin main`은 터미널의 GitHub 인증 정보 부재로 실패했다.
- 연결된 GitHub 도구의 저장소 쓰기 권한을 확인하고 같은 파일 트리로 커밋을 만든 뒤 main을 fast-forward했다. 강제 push는 하지 않았다.
- GitHub 반영 완료 커밋: `86cf5894d7576ca7f45f14cf070a4a4d7a064580`.
- 커밋 메시지: `feat: complete mission review docs and route flow coverage`.
- `git fetch origin`, `git rebase origin/main` 성공. 동일한 변경의 로컬 커밋은 Git이 자동으로 생략했다. 로컬·원격 차이 0/0, 작업 트리 깨끗함 확인.
- `npm run format:check`, `git diff --check` 통과. 코드 변경이 없으므로 앞서 통과한 테스트 23개와 빌드는 반복하지 않았다.
- 이 결과 기록은 `docs: record successful GitHub publication` 후속 커밋으로 함께 반영한다.
- 남은 사항: 자동 재배포 성공 여부는 이번 push 작업에서 검증하지 않았다.

## 2026-09-07 19:15 KST — push 후 자동 배포 확인

- 요청: 변경 반영 후 Vercel 재배포 여부를 확인해야 하는지 질문. 제출 준비를 위해 실제 상태 확인까지 수행.
- 수정 파일: `WORK_LOG.md`에 확인 결과만 추가.
- 확인 대상: main 커밋 `1ccb319e538e86cadea8abe8d128b0df7f6da48b`.
- GitHub 커밋 상태: Vercel `success`, 배포 URL https://vercel.com/dooolll00/b1-2/Dh5dbq8epWKAJKRircqTKjKmHXQg .
- GitHub Actions: `React checks` completed/success, https://github.com/dooolll00/B1-2/actions/runs/34110436898 .
- 실제 공개 URL을 Chromium으로 확인: `/items/new`, `/guide`, 임의 404 경로 모두 HTTP 200 및 예상 화면 표시. SPA의 404 안내는 앱 화면 기준이며 HTTP 404 응답을 의미하지 않음.
- 브라우저 미처리 오류 0개. 새 계정/학습 기록 생성 없이 공개 페이지 점검만 수행. 원격 CRUD는 앞선 검증 결과 참고.
- 해결되지 않은 배포 문제 없음. 다음 작업은 발표 가이드에 따라 시연 연습.
- 이번 확인 기록은 로컬에만 추가했으며 추가 commit/push는 수행하지 않음.

## 2026-09-07 20:26 KST — B1-1 디자인·폰트 적용

- 요청: B1-1처럼 디자인과 폰트를 변경하되 미션 요구사항에 영향이 없는지 검토 후 적용.
- 변경 파일: `src/styles.css`, `src/components/ThemeToggle.jsx` 추가, `src/components/Layout.jsx`, `index.html`, `README.md`, `PRESENTATION_GUIDE.md`, `WORK_LOG.md`.
- B1-1의 WORK_LOG 및 CSS를 참고만 했으며 해당 프로젝트 파일은 수정하지 않았다. 기존 B1-2 작업 기록 미커밋 변경도 보존했다.
- 핵심 작업: Pretendard CDN/시스템 폰트 fallback, 블루·회색 라이트/네이비·퍼플 다크 테마, 카드 10px 모서리와 그림자, 반응형 헤더 전환 버튼 적용. 다크 포인트·보조 글자는 대비를 위해 밝게 조정.
- 테마는 React state/effect로 관리. 사용자가 선택하기 전에는 시스템 테마를 따르며 선택 후 `today-learning-theme`만 localStorage에 저장한다. 저장소 오류는 처리하여 UI 이용을 유지한다.
- 미션 검토: CSS 자유 선택 범위이며 React 라우트·공통 컴포넌트·원격 CRUD·폼 검증·비동기 상태 구조는 유지한다. 로딩·오류·빈 상태·입력 오류·포커스·비활성 표시에도 테마 적용.
- 테스트: `npm test` 23개 통과, `npm run build` 통과. 임시 Node 22.14.0 환경 사용.
- 브라우저: localhost:5174와 `/tmp/b1-2-browser/design.cjs` 사용. 원격 API 대신 브라우저 모듈 응답 fixture로 화면 데이터 구성. 라이트/다크 각각 7경로 × 320/390/768/1440px에서 가로 넘침 없음. Pretendard 로딩, 테마 새로고침 유지, 모의 CRUD/폼 검증, CDN 및 localStorage 차단 fallback 통과. 미처리 JS 오류 0.
- 화면: `/tmp/b1-2-light.png`, `/tmp/b1-2-dark.png`, `/tmp/b1-2-mobile.png` 캡처를 열어 확인. 실제 원격 데이터·인증 설정은 변경하지 않음.
- 남은 사항/다음 작업: 디자인은 로컬에만 적용. 이번 변경의 commit/push/배포는 수행하지 않았다. 배포 시 공개본의 폰트·테마·주요 경로를 확인한다.

## 2026-09-09 14:33 KST — B1-2 작업 재개 맥락 확인

- 요청: B1-2 코딩을 이어서 진행.
- 수정 파일: `WORK_LOG.md`에 재개 기록 추가. 앱 코드 수정 없음.
- 확인: 작업 기록, AGENTS.md, package.json, `git status --short`, `git log -3 --oneline` 확인. 시작 시 작업 트리는 깨끗함.
- 현재 디자인·폰트·테마 변경은 로컬 커밋 `1b97d1e`에 포함됨. 이전 기록의 미커밋 상태 이후 커밋된 것으로 확인. 원격 반영·배포 상태는 이번에 확인하지 않음.
- 테스트: 코드 변경이 없어 실행하지 않음. 이번 작업에서 commit/push 수행 없음.
- 남은 사항/다음 작업: 사용자가 이어서 수정할 기능이나 화면을 지정하면 B1-2에서 진행.

## 2026-09-09 14:38 KST — 데이터 전환·삭제 상태 오류 및 테스트 도구 취약점 수정

- 요청: B1-2에서 수정이 필요한 부분을 점검하고 수정.
- 수정 파일: `src/hooks/useRecords.js`, `src/lib/records.js`, `src/pages/Detail.jsx`, `src/test/flows.test.jsx`, `src/test/records.test.js`(추가), `package.json`, `package-lock.json`, `WORK_LOG.md`.
- 핵심 작업: 조회 함수나 재시도 번호가 바뀐 첫 렌더부터 이전 데이터를 숨겨 새 기록 ID와 이전 데이터가 섞이는 것을 방지. 상세 컴포넌트를 기록 ID별로 초기화하여 삭제 확인·오류 상태의 전이를 방지. 삭제 취소 시 오류도 초기화. 잘못된 UUID는 인증 요청 전에 없는 기록으로 처리하여 불필요한 익명 로그인과 연결 오류를 방지.
- 회귀 검증: 데이터 전환 첫 렌더, 다른 기록 이동 시 삭제 상태 초기화, 삭제 실패 후 취소·재시도, 잘못된 ID의 인증 생략과 유효 ID의 인증 오류 전달 테스트 추가.
- 의존성: npm audit에서 Vitest 및 @vitest/mocker 중간 등급 취약점 2개 확인. Node 22 및 Vite 6 호환 정보를 확인한 뒤 Vitest 4.1.11로 업데이트. package-lock.json 갱신.
- 명령·결과: 시스템 Node 부재로 `/tmp/node-v22.14.0-darwin-x64/bin`의 임시 Node 22.14.0 사용. npm 10.9.2 및 10.9.4의 업데이트 명령은 내부 edgesOut 오류로 실패하여 임시 npm 11.6.2로 업데이트 성공. 이후 기본 npm 10.9.2로 `npm ci` 성공, `npm test` 31개 통과, `npm run build` 성공, `npm audit` 취약점 0개. 앱 수정 후 최초 형식 검사도 통과.
- 남은 사항: 실제 브라우저와 원격 Supabase CRUD·배포는 이번에 재검증하지 않음. 조회 한도에 따른 대량 기록 검색·통계 제한은 기존 범위 유지.
- 다음 작업: 필요 시 공개 배포 반영 후 주요 화면·CRUD 확인. 이번 작업에서 Git commit/push 및 배포는 수행하지 않음.

## 2026-09-09 — 전달받은 미션 원문과 B1-2 기준 불일치 확인

- 요청: 미션 원문 전달 및 기존 미션 이해 확인.
- 확인 결과: 전달된 원문은 외부 라이브러리 없이 HTML/CSS/JavaScript로 만드는 포트폴리오 미션. GitHub 저장소 API, 6개 섹션, DOM 이벤트, GitHub Pages 배포를 요구하며 React 사용을 금지함. 기존 B1-2 README의 React SPA 미션과 서로 다른 기준임.
- 수정 파일: `WORK_LOG.md`만 추가. 첨부 원문을 읽었으며 앱 코드 변경·테스트·commit/push 없음.
- 남은 사항/다음 작업: 사용자가 다른 미션 원문을 전달한 것인지, B1-2의 목표를 이 포트폴리오 미션으로 바꾸려는 것인지 확인 필요. 확인 전 기존 React 앱을 변환하지 않음.

## 2026-09-09 14:40 KST — B1-2 React SPA 미션 원문 확정

- 요청: 사용자가 B1-2의 React SPA 미션 원문을 다시 제공.
- 확정 기준: React 18 이상, 5개 이상 라우트와 Not Found, props 기반 재사용 컴포넌트 8개 이상, 공통 레이아웃, pages/components/hooks 또는 lib 분리, controlled input 및 커스텀 조회·갱신 훅, Supabase/Firebase 원격 CRUD, 폼 검증·제출 중·실패 UI, 일관된 로딩·오류·빈 상태, 이벤트→상태→렌더링 3개 이상, 공개 배포 CRUD 정상 동작, README 실행 방법·기술 스택·제출 링크, 환경변수 관리.
- 보너스: Context 등 전역 상태, 메모이제이션, 로그인과 보호 라우트. JavaScript·순수 CSS 허용, 반응형은 선택. UI 고도화보다 React 구조와 데이터 흐름을 우선한다.
- 결정: 직전 포트폴리오 미션은 B1-2 적용 기준으로 사용하지 않음. 이번 원문은 기존 README 체크리스트와 일치하며 앞선 버그 수정 방향을 유지함.
- 수정 파일: `WORK_LOG.md`만 추가. 원문과 기존 체크리스트 대조, 코드 변경·테스트·commit/push 없음.
- 남은 사항/다음 작업: 최신 로컬 수정의 배포 반영과 공개 환경 CRUD는 아직 재검증하지 않음. 인증 보너스는 보호 라우트가 없어 부분 구현. 이후 작업에서도 이번 원문 기준을 유지한다.

## 2026-09-09 14:41 KST — 미션 상세 정리 및 기록 위치 지정

- 사용자 요청: B1-2 미션 내용을 WORK_LOG.md에 정리하고 README나 다른 파일에는 넣지 않는다.
- 수정 파일: `WORK_LOG.md`만 수정. 이전 기록 뒤에 추가하며 기존 README와 다른 파일은 변경하지 않는다.
- 기록 원칙: 앞으로 미션 원문·요구사항 정리는 이 파일에서 관리한다. 아래 내용은 사용자가 이번 대화에서 제공한 React SPA 미션을 요약한 요구사항이며, 구현 완료 판정과 구분한다.

### 미션 목적과 학습 목표

React 기반 SPA 서비스 하나를 완성하며 컴포넌트 구조와 데이터 흐름을 익힌다. 화면의 화려함보다 사용자 이벤트 → 상태 변화 → 렌더링 변화, 상태의 소유 위치, 비동기 UI의 완성도를 우선한다. 백엔드는 Supabase 또는 Firebase를 최소 범위로 사용하며 백엔드 구현 자체가 평가의 중심은 아니다.

완료 후 다음을 스스로 설명할 수 있어야 한다.

- 컴포넌트가 필요한 이유와 본인이 컴포넌트를 나눈 기준.
- props와 state의 차이, 상태를 둔 위치와 상향·하향 데이터 흐름.
- useEffect 실행 시점, 의존성, 데이터 요청과의 관계.
- 비동기 요청의 로딩·성공·실패·빈 상태를 React 화면에 표현하는 방법.
- 한 기능에서 라우팅 → 컴포넌트 → 상태 → 이벤트 → 렌더링이 연결되는 과정.

### 필수 요구사항

1. 프로젝트 구성
   - React 18 이상으로 구현한다.
   - 서비스 주제는 자유지만 단일 핵심 데이터의 CRUD가 가능한 범위로 제한한다.
   - `pages`, `components`, `hooks` 또는 `lib`의 역할을 분리한다.
   - 주요 페이지에 공통 헤더·네비게이션 레이아웃을 적용한다.
2. 라우팅
   - React Router 등으로 최소 5개 이상의 페이지 라우트를 제공한다.
   - 목록과 상세 라우트를 반드시 포함하고, 잘못된 주소에는 Not Found 페이지를 표시한다.
   - 네비게이션 등 링크로 주요 라우트에 접근할 수 있어야 한다.
   - 원문의 `/login`, `/profile` 등은 예시이며 해당 이름의 페이지 자체가 필수는 아니다.
3. 컴포넌트 설계
   - 재사용 컴포넌트를 최소 8개 이상 만든다.
   - 재사용 컴포넌트는 최소 1개 이상의 prop에 따라 동작이나 표시가 달라져야 한다.
   - 페이지 컴포넌트와 UI 컴포넌트를 분리한다.
   - 로딩·에러·빈 상태는 공통 컴포넌트로 통일한다.
4. 상태 관리
   - 폼 입력을 controlled input으로 관리한다.
   - 목록·상세 데이터 및 로딩·에러 상태를 React 방식으로 관리하고 컴포넌트별로 적절히 분리한다.
   - 데이터 조회 또는 갱신 흐름 중 최소 1개를 커스텀 훅으로 분리한다.
5. 원격 CRUD
   - Supabase 또는 Firebase의 원격 데이터를 기준으로 동작해야 한다.
   - 목록 조회 결과를 리스트 UI로 표시한다.
   - 상세는 라우트 파라미터로 특정 데이터를 조회한다.
   - 등록·수정은 폼 제출 후 성공 시 이동하거나 화면을 갱신한다.
   - 삭제 후 목록 갱신 또는 이동 흐름을 제공한다.
6. 폼 UX
   - 등록·수정 폼에 필수값 검증을 구현한다.
   - 검증 오류를 입력 필드 근처 또는 상단에 표시한다.
   - 제출 중 버튼 비활성화나 스피너 등으로 진행 상태를 표시한다.
   - 네트워크·권한·기타 요청 실패를 화면에서 알린다.
7. 이벤트와 렌더링
   - 클릭·입력·제출 등의 이벤트가 상태 변화와 렌더링으로 이어져야 한다.
   - 최소 3개 이상의 흐름이 명확해야 한다. 예: 필터 변경 → 목록 변경, 입력 → 미리보기 변경, 저장 성공 → 알림 표시.
8. 배포와 제출
   - Vercel/Netlify 등 외부에서 접속 가능한 URL로 배포한다.
   - 배포 환경에서 목록·상세·등록·수정·삭제가 모두 정상 동작해야 한다.
   - 환경변수 등 설정 누락으로 일부 기능이 실패하면 요구사항을 충족하지 못한다.
   - 제출물은 GitHub 저장소 URL과 배포 사이트 URL이다.
   - README에는 로컬 설치·실행 명령과 사용 기술 스택이 필요하다. 이는 미션의 README 요구사항이며, 미션 내용 자체를 README에 추가하라는 뜻은 아니다.

### 선택 과제와 허용 범위

- 전역 상태 보너스: 로그인 사용자·테마·알림 중 하나를 Context 등으로 관리한다.
- 성능 최적화 보너스: useMemo/useCallback/React.memo 중 하나 이상을 적용한다.
- 인증 보너스: Supabase/Firebase Auth 로그인 흐름과 보호 라우트를 함께 구현한다.
- JavaScript만 사용해도 된다. TypeScript는 선택이며 사용 자체에 가산점은 없다.
- 순수 CSS, CSS Modules, Tailwind CSS, CSS-in-JS, UI 라이브러리 등 스타일링 방식은 자유다.
- 반응형 디자인은 선택이다.
- 고급 권한/RLS/Rules, 복잡한 데이터 관계 설계는 필수가 아니다. 별도 백엔드 서버 직접 구현도 요구하지 않는다.

### 환경변수와 보안 요구사항

- API Key 등 민감정보는 `.env`로 관리하고 `.gitignore`에 `.env`가 포함되어 있는지 확인한다.
- API Key가 포함된 코드를 GitHub에 push하지 않는다.
- 배포 환경의 변수는 Vercel/Netlify 등의 Environment Variables에 별도로 등록한다.
- 실제 키·토큰·환경변수 비밀값은 이 작업 기록에도 쓰지 않는다.

### 결과 확인 예시와 현재 후속 작업

- 목록 진입 시 로딩 표시 후 카드가 나타나고, 항목 클릭 시 상세로 이동한다.
- 등록 폼 저장 성공 시 목록 또는 상세로 이동한다.
- 빈 데이터와 요청 실패를 각각 구분해 안내한다. 원문의 화면 문구와 디자인은 예시이므로 동일하게 만들 필요는 없다.
- 현재 B1-2의 최근 로컬 검증은 테스트 31개·빌드 통과이며, 최신 변경의 배포 반영과 원격 CRUD 재검증은 남아 있다.
- 인증 보너스는 익명 Auth만 있고 보호 라우트가 없어 부분 구현 상태다.
- 이번 작업은 문서 정리만 수행. 명령: 기존 WORK_LOG 읽기, 기록 추가, `git diff --check -- WORK_LOG.md`로 공백 오류 검사. 앱 테스트는 코드 변경이 없어 재실행하지 않는다.
- 해결되지 않은 미션 기준 혼선 없음. 다음 작업은 이 기준에 따라 기능을 유지·보완하고 필요한 배포 검증을 진행한다. 이번 작업에서 commit/push 없음.

## 2026-09-09 — 최신 수정본 배포 및 실제 CRUD 검증 시작

- 요청: 최신 수정본을 배포하고 실제 배포 환경에서 CRUD 확인.
- 배포 대상: 앞서 완료한 조회·삭제 상태 수정, 잘못된 ID 처리, Vitest 업데이트 및 회귀 테스트, 누적 작업 기록. README와 다른 설명 문서에 미션 내용을 추가하지 않는다.
- 사전 확인: 원격 fetch 성공, 로컬 HEAD와 origin/main 차이 0/0. 테스트 31개·빌드·audit 통과 결과 유지. 배포 직전 format:check와 git diff --check 통과.
- 커밋 메시지: `fix: isolate record request state and update test dependencies`.
- 다음 작업: GitHub main 반영, Vercel 배포 상태 및 공개 사이트에서 테스트 기록 생성·조회·수정·삭제 검증. 기존 사용자 기록은 건드리지 않고 검증 기록만 삭제한다. 실제 결과는 후속 기록에 추가한다.

## 2026-09-09 14:47 KST — 최신 배포 및 실제 배포 환경 CRUD 검증 완료

- 요청: 최신 수정본 배포와 실제 공개 환경 CRUD 확인.
- 배포 URL: https://b1-2-steel.vercel.app
- 최종 배포 커밋: `d4f1822fcff7cd35b18266615bdf16e0ad0371f6`.
- GitHub 반영: 터미널 push는 기존 VS Code 인증 소켓 연결 실패로 실패. 연결된 GitHub 도구로 main을 fast-forward하여 반영했다. 강제 push 없음.
- 커밋 메시지: `fix: isolate record request state and update test dependencies` (`d12de15`), `fix: preserve complete dependency lockfile during publication` (`d4f1822`). 최초 전송에서 도구 출력 길이 제한으로 package-lock.json 내용 일부가 잘린 것을 원격·로컬 diff로 발견해 즉시 복구했다. 첫 커밋의 CI는 실패했고, 최종 커밋의 CI는 성공했다.
- 파일 동일성: 복구 후 원격 tree SHA `f918085ca6661e087593f77868111999f897e75b`가 로컬 검증본과 일치함을 확인. fetch 및 rebase로 중복 로컬 커밋을 정리했고 최종 HEAD와 origin/main 차이는 0/0. 이 결과 기록 추가 직전 작업 트리는 깨끗했다.
- Vercel 상태: 최종 커밋 `success`, https://vercel.com/dooolll00/b1-2/EcYR6gZXX6YxqTrPNtUXanKQdBrc .
- GitHub Actions: 최종 커밋의 React checks completed/success, https://github.com/dooolll00/B1-2/actions/runs/34316170828 .
- 검증 도구: 임시 Node 22.14.0 및 `/tmp/b1-2-browser`에 설치한 Playwright/Chromium. 프로젝트 의존성은 이번 배포 검증을 위해 추가 변경하지 않음. 실행 명령은 `node /tmp/b1-2-browser/verify.cjs`.
- 실제 공개 사이트 검증 14개 통과: 목록·빈 상태, 최신 조회 상태 가드가 포함된 배포 번들, 필수값 오류 시 원격 저장 차단, 등록 후 상세 이동, 상세 새로고침, 제목·상태 수정 후 새로고침 영속성, 목록 검색·초기화·상태 필터, 삭제 취소 후 기록 유지, 오프라인 조회 오류와 온라인 재시도, 삭제 후 목록 빈 상태 및 상세 부재, 잘못된 UUID 접근 시 인증 요청 0개, SPA 404, 테마 새로고침 유지와 390px 등록 폼 가로 넘침 없음, 원격 쓰기 성공 및 미처리 브라우저 오류 없음.
- 원격 응답: 등록 POST 201, 수정 PATCH 200, 삭제 DELETE 200. 모의 API를 사용하지 않고 배포 사이트가 실제 Supabase에 요청하는 흐름을 검증했다.
- 테스트 스크립트 첫 실행은 동일한 이름의 새 기록 링크가 2개여서 선택자 오류로 중단되었으며, 당시 원격 기록 쓰기는 없었다. 선택자를 수정한 두 번째 실행에서 전체 통과. 앱 오류는 발견되지 않았다.
- 데이터 정리: 생성한 검증 기록 1개 삭제 완료, 기존 사용자 기록 수정·삭제 없음. 두 번의 목록 검증으로 생성된 익명 Auth 테스트 계정은 서버에 남으며 삭제하지 않았다. 잘못된 ID 검증용 별도 브라우저에서는 인증 요청이 없었다.
- 임시 결과: `/tmp/b1-2-browser/result.json`, `/tmp/b1-2-browser/production-mobile.png`. 임시 경로는 이후 사라질 수 있으므로 핵심 결과는 이 기록에 보존한다.
- 수정 파일: 기존 승인된 코드·테스트·의존성·누적 작업 기록 8개 파일을 GitHub에 반영. 검증 완료 후에는 `WORK_LOG.md`에만 결과 추가. README 및 다른 설명 문서는 변경하지 않음.
- 남은 문제: 이번 검증 범위에서 배포 및 CRUD 문제 없음. 사용자 간 권한 위반 쓰기와 실제 저장 실패 상황은 이번 원격 검증에 포함하지 않았으며, 저장 실패 UI는 앞선 자동 테스트로 확인했다.
- 다음 작업: 현재 공개 URL로 제출·시연 가능. 이번 최종 결과 기록은 로컬에 추가했으며 결과 기록만을 위한 추가 commit/push는 하지 않음.

## 2026-09-09 14:50 KST — README 검증·배포 현황 최신화

- 요청: README에도 수정할 사항이 없는지 확인.
- 확인: 테스트 23개, 9월 7일 최종 검증, 코드·디자인 미배포 안내가 최신 상태와 불일치했다. 앞선 사용자의 제한은 미션 내용을 README 등 다른 파일에 추가하지 말라는 것으로, 일반 운영·검증 현황 갱신과 구분한다.
- 수정 파일: `README.md`, `WORK_LOG.md`.
- 작업: 테스트 31개, 9월 9일 실제 CRUD 검증, 최신 배포 커밋과 CI 성공, Vitest 업데이트 및 audit 결과, 디자인 배포 상태 반영. 이번에 재검증하지 않은 사용자 분리·소스 URL·키 패턴 검사는 9월 7일 결과임을 명시. 미션 요구사항 내용은 추가·변경하지 않음.
- 검증: README Prettier 정렬 및 `git diff --check` 수행. 코드 변경이 없어 앱 테스트·빌드는 반복하지 않음.
- 남은 사항/다음 작업: 문서 최신화와 직전 배포 결과 기록은 로컬에만 반영. 이번 작업에서 추가 commit/push·배포는 하지 않음. 앱 배포 및 CRUD 검증은 앞선 완료 상태 유지.

## 2026-09-09 — README 및 배포 검증 기록 GitHub 반영

- 요청: 최신 문서 변경을 push.
- 변경 파일: `README.md`, `WORK_LOG.md`. 미션 내용 추가 없이 README의 검증·배포 현황과 누적 작업 기록을 반영한다.
- 검증: fetch 성공, 로컬·원격 차이 0/0, git diff --check 통과. 문서 변경만 있으므로 앱 테스트·빌드는 반복하지 않음.
- 반영 방법: 이번 세션에서 확인한 터미널 인증 소켓 오류를 피하기 위해 연결된 GitHub 도구로 동일한 파일 트리를 생성하고 main을 fast-forward한다. 강제 push 없음.
- 커밋 메시지: `docs: update deployment verification and work log`.
- push 상태/다음 작업: 아래 후속 결과에 실제 반영 및 동기화 결과를 기록한다.
