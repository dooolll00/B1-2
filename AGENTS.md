# Codex 작업 기록 규칙

이 저장소에서 작업하는 Codex는 다음 규칙을 항상 따른다.

1. 작업을 시작하기 전에 `WORK_LOG.md`를 먼저 읽어 이전 작업 맥락과 남은 일을 확인한다.
2. 기존 프로젝트 구조와 사용자의 작업을 임의로 초기화하거나 삭제하지 않는다.
3. 사용자의 요청을 수행한 뒤, 작업을 종료하기 전에 반드시 `WORK_LOG.md` 맨 아래에 새 기록을 추가한다.
4. 이전 기록은 삭제하거나 덮어쓰지 않고 항상 이어서 추가한다.
5. 새 기록에는 날짜/시간, 요청 요약, 수정 파일, 핵심 작업, 테스트/명령 결과, 해결되지 않은 문제, 다음 할 일을 포함한다.
6. 중요한 결정과 배포/환경 설정도 다음 작업에서 필요하면 기록한다.
7. 비밀번호, 토큰, Supabase 키, `.env` 실제 값 등 민감정보는 절대 기록하지 않는다.
8. 사용자가 설명을 요청하면 비전공자도 이해하기 쉬운 한국어로 설명한다.
9. Git commit/push를 수행했다면 커밋 메시지와 push 여부를 기록한다.
10. 작업이 없거나 단순 질문만 받은 경우에도 다음 작업에 필요한 맥락이 생겼다면 짧게 기록한다.

## 목적

Codex 채팅 목록이 사라지거나 다른 환경에서 저장소를 다시 열어도 `WORK_LOG.md`를 읽으면 이전 작업 상태를 이어갈 수 있도록 한다.
