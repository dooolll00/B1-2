# B1-2 발표 가이드 — 오늘의 배움

이 문서는 제공받은 B1-2 미션 원문과 현재 코드 기준입니다. 평가의 핵심인 컴포넌트 분리와 상태·이벤트·비동기 렌더링 흐름을 중심으로 설명합니다.

## 3~5분 발표 대본

### 1. 무엇을 만들었나요? (30초)

“오늘 배운 내용을 기록하고 다시 찾아볼 수 있는 학습 기록 서비스입니다. 제목과 내용, 주제와 학습 상태를 저장하고 검색·수정·삭제할 수 있습니다. React로 화면을 만들고 Supabase 데이터베이스에 기록을 저장했습니다.”

### 2. 화면과 컴포넌트는 어떻게 나눴나요? (40초)

“주소에 따라 목록, 등록, 상세, 수정 화면을 보여줍니다. 페이지는 데이터 조회와 화면 이동을 담당하고, 공통 컴포넌트는 버튼·카드·폼처럼 반복되는 화면을 담당합니다. 등록과 수정에서 같은 RecordForm을 사용하므로 입력 규칙과 오류 표시가 일관됩니다.”

코드: `src/App.jsx` → `src/pages/Editor.jsx` → `src/components/RecordForm.jsx`.

### 3. 입력하면 화면이 왜 바로 바뀌나요? (40초)

“입력 내용은 state라는 화면의 기억에 보관됩니다. 입력 이벤트가 state를 바꾸면 React가 그 값으로 화면을 다시 그립니다. 입력창과 미리보기가 같은 values를 사용하므로 동시에 바뀝니다. 수정할 때는 부모 페이지가 기존 기록을 initial이라는 props로 폼에 전달합니다.”

코드: `RecordForm`의 `values`, `change`, `initial`, `onSubmit`.

### 4. 저장은 어떤 순서로 이루어지나요? (60초)

“저장 버튼을 누르면 빈 값과 길이를 검사합니다. 통과하면 버튼과 입력을 잠그고 부모 페이지의 저장 함수를 호출합니다. 이 함수가 데이터베이스 저장을 기다린 다음, 성공하면 상세 화면으로 이동하고 완료 알림을 보여줍니다. 실패하면 이동하지 않고 오류와 기존 입력을 유지합니다.”

```text
입력 이벤트 → values 변경 → 입력창·미리보기 갱신
제출 → 검증 → pending/lock → Editor의 onSubmit
     → saveRecord → ensureSession → Supabase
     → 성공: 상세 이동 + Context 알림
     → 실패: 폼 오류 표시 + 입력 유지 + 다시 제출 가능
```

`pending`은 화면을 비활성화하는 상태이고, `lock`은 렌더링을 기다리지 않고 연속 요청을 막는 ref입니다. 검증은 폼뿐 아니라 저장 함수와 DB 제약에도 있습니다.

### 5. 비동기 요청과 접근 권한은 어떻게 처리했나요? (50초)

“조회 중에는 로딩, 실패하면 오류와 재시도, 결과가 없으면 빈 화면 안내를 보여줍니다. 주소가 바뀐 뒤 예전 요청이 늦게 도착해도 새 화면을 덮어쓰지 않도록 이전 결과를 무시합니다. 사용자는 익명 로그인을 하고, 데이터베이스 정책이 본인 소유의 행만 허용합니다.”

코드: `src/hooks/useRecords.js`, `src/lib/supabase.js`, `supabase/schema.sql`.

“자동 테스트는 원격 API를 가짜 응답으로 바꾸어 React 동작을 확인합니다. 실제 서버와 배포 상태는 별도로 확인해야 합니다.”

## 시연 순서

시연 전 같은 브라우저 세션을 유지하고 연결 상태를 확인하세요. 직접 만든 시연 기록만 수정·삭제하세요.

| 순서 | 조작                                  | 보여줄 결과와 설명                     |
| ---- | ------------------------------------- | -------------------------------------- |
| 1    | 목록 → 새 기록 → 빈 폼 저장           | 제목·내용 오류, 저장 요청 차단         |
| 2    | 제목 `발표용 React 상태`, 내용 입력   | 미리보기와 글자 수 즉시 갱신           |
| 3    | 저장 → 상세 → 새로고침                | 상세 이동, 완료 알림, 원격 기록 재조회 |
| 4    | 목록 → 검색·주제·상태 변경            | 조건에 맞는 기록, 결과 없음, 초기화    |
| 5    | 상세 → 수정 → 상태와 내용 변경 → 저장 | 초기값 표시, 저장 후 수정 내용 확인    |
| 6    | 삭제 → 취소 → 다시 삭제 확인          | 취소 시 유지, 확인 후 목록 이동        |
| 7    | 임의 경로 접속                        | 404 안내                               |

시간이 있으면 개발자 도구에서 Offline 전환 후 목록에 다시 진입해 오류를 보여주고, Online 복구 후 다시 시도를 누르세요. 네트워크 오류 확인은 시연 전에 연습하세요.

## 예상 질문과 답변

**SPA가 무엇인가요?** 화면을 이동할 때마다 새 HTML 문서를 받는 대신 React가 주소에 맞는 화면을 바꾸는 방식입니다. 데이터 요청은 별도로 일어납니다. 상세 URL을 새로고침할 때는 서버도 앱의 HTML을 반환해야 하므로 배포 경로 설정이 필요합니다.

**props와 state는 어떻게 다른가요?** props는 부모에게 받은 값이나 함수이고, state는 컴포넌트가 기억하고 바꾸는 값입니다. `initial`과 `onSubmit`은 props, `values`와 `pending`은 state입니다.

**useEffect는 왜 쓰나요?** 화면이 나타나거나 조회 대상이 바뀔 때 외부 데이터를 요청하기 위해 사용합니다. `useRemote`는 loader와 재시도 번호가 바뀌면 다시 조회합니다. cleanup은 요청 자체를 취소하지 않고 오래된 결과의 반영을 막습니다.

**Context는 무엇에 쓰나요?** 저장·삭제 후 화면이 바뀌어도 완료 알림을 공유하도록 사용했습니다. 모든 폼 상태나 원격 데이터를 Context에 넣은 것은 아닙니다.

**useMemo와 useCallback을 왜 썼나요?** useMemo는 데이터와 검색 조건이 같으면 계산된 필터 결과를 재사용합니다. useCallback은 id가 같을 때 조회 함수의 참조를 유지하여 불필요한 effect 재실행을 막습니다. 실제 속도 개선을 측정한 것은 아닙니다.

**localStorage를 사용하나요?** 학습 기록 CRUD 저장소는 Supabase입니다. 인증 SDK가 브라우저에 로그인 세션을 보관할 수 있으므로 “localStorage를 전혀 사용하지 않는다”고 설명하면 부정확합니다.

**다른 사람 기록은 어떻게 막나요?** SQL의 RLS 정책이 로그인 사용자의 ID와 행의 user_id를 비교합니다. 화면에서 숨기는 것만으로 보호하는 구조는 아닙니다. 실제 적용 상태는 서로 다른 세션으로 검증해야 합니다.

**익명 로그인의 한계는 무엇인가요?** 브라우저 데이터를 지우거나 기기를 바꾸면 같은 계정으로 복구할 수 없습니다. 영구 계정 로그인, 보호 라우트, 계정 복구는 현재 구현하지 않았습니다.

**규모가 커져도 괜찮나요?** 현재 목록은 조회한 데이터 안에서 검색하고 통계를 계산합니다. 페이지네이션이나 서버 검색은 없습니다. 데이터가 많아지면 API 조회 한도와 성능 때문에 서버 검색·집계·페이지네이션을 추가해야 합니다.

**테스트가 통과하면 완벽한가요?** 아닙니다. 현재 자동 테스트는 mock 기반이며 실제 RLS, 인증 설정, 상세 새로고침, 모바일 배치와 배포 환경을 증명하지 않습니다. README에 미션 원문과 대조한 구현 근거를 정리했습니다.

## 발표 직전 준비

- README의 요구사항 체크리스트와 최종 검증 결과를 확인합니다.
- `npm ci`, `npm test`, `npm run build`, `npm run format:check` 결과를 확인합니다.
- 공개 배포 URL을 열고 CRUD와 상세 새로고침을 같은 세션에서 점검합니다.
- 코드 탭은 `RecordForm.jsx`, `Editor.jsx`, `records.js`, `useRecords.js`, `schema.sql` 순서로 준비합니다.
- 환경변수 실제 값이나 인증 토큰이 화면에 노출되지 않도록 합니다.

## 상태를 어디에 두었는지 설명하기

| 상태                               | 소유 위치                 | 그 위치에 둔 이유                                                  |
| ---------------------------------- | ------------------------- | ------------------------------------------------------------------ |
| 입력값·필드 오류·제출 중·저장 실패 | RecordForm                | 입력창과 미리보기가 함께 사용하는 상태이므로 공통 부모인 폼이 소유 |
| 검색어·주제·상태 필터              | Items                     | 목록 화면 안에서만 필요                                            |
| 조회 결과·로딩·오류·재시도 번호    | useRemote를 호출한 페이지 | 여러 화면이 같은 조회 규칙을 재사용하되 각각의 조회 상태를 소유    |
| 삭제 확인·삭제 중·삭제 실패        | Detail                    | 상세 화면의 삭제 동작에만 필요                                     |
| 완료 알림                          | ToastProvider             | 화면 이동 뒤에도 여러 페이지가 공유해야 함                         |
| 원본 학습 기록                     | Supabase                  | 새로고침 이후에도 유지되어야 함                                    |

“자식은 props를 직접 바꾸지 않고 부모가 전달한 콜백을 호출합니다. RecordForm이 onSubmit(values)를 호출하면 Editor가 저장하고 이동합니다. useRecords를 두 페이지가 쓴다고 상태까지 자동으로 공유되는 것은 아닙니다. 각 호출은 별도의 state를 갖습니다.”

## useEffect와 렌더링을 정확히 설명하기

- `useRemote`의 의존성은 `[loader, revision]`입니다. 첫 마운트, loader 변경, 재시도 번호 증가 때 실행됩니다.
- `useRecord`의 loader는 `[id]`에 의존합니다. 상세 대상이 바뀌면 다시 조회합니다.
- `setState`로 로딩 → 성공/실패 상태가 바뀌면 호출한 페이지가 다시 렌더링되어 공통 상태 UI 또는 데이터를 보여줍니다.
- 렌더링은 JSX 결과를 다시 계산하는 과정이고, 실제 DOM은 필요한 부분만 반영됩니다. 이벤트가 발생했다는 이유만으로 항상 전체 DOM을 새로 만드는 것은 아닙니다.
- 개발용 StrictMode에서는 effect의 설정·정리가 추가 실행될 수 있습니다. `active` cleanup은 오래된 응답 반영을 막지만 네트워크 요청 횟수를 하나로 만드는 장치는 아닙니다.
- `ToastProvider`의 effect는 `[toast]`에 의존하며 4.5초 후 알림을 지웁니다. `Layout`의 effect는 `[pathname]`에 의존하며 이동 후 스크롤과 본문 포커스를 조정합니다.

## 평가 기준에 맞춘 마무리 발언

“6개 페이지와 404 라우트를 만들었고, prop을 받는 공통 컴포넌트 12개로 UI를 분리했습니다. 폼은 입력 상태, 페이지는 이동과 기능 조합, 커스텀 훅은 조회 상태를 담당합니다. 원격 CRUD와 로딩·실패·빈 상태를 연결하고, 입력·검색·저장 이벤트가 화면 변화로 이어지는 것을 확인했습니다. 보너스는 전역 알림과 메모이제이션을 적용했습니다. 익명 인증은 사용하지만 보호 라우트가 없어서 인증 보너스를 완성했다고 설명하지는 않습니다.”

## 디자인 변경 설명

“B1-1과 시각적인 통일감을 주기 위해 Pretendard와 블루·퍼플 테마를 적용했습니다. 스타일은 CSS 변수로 관리하고, 테마 선택은 ThemeToggle의 React state로 관리합니다. 선택값은 브라우저에 저장하지만 학습 기록은 Supabase에 저장하므로 데이터 흐름은 그대로입니다. 사용자가 직접 선택하기 전에는 시스템 테마 변경을 따릅니다.”

테마 버튼 클릭 → preference 변경 → theme 계산 → effect가 루트의 data-theme 변경 → CSS 변수에 따라 전체 화면 색상이 바뀌는 흐름도 설명할 수 있습니다.
